import { FC } from "react";
import { Column } from "../../reusable-components/Column";

const ScheduleScreen: FC = () => {
  return <Column>SCHEDULE SCREEN</Column>;
};

export default ScheduleScreen;
